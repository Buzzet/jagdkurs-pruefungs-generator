services:
  jagdkurs-ai-api:
    image: jagdkurs-ai-api:latest
    container_name: jagdkurs-ai-api
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "18080:8080"
    networks:
      - web

networks:
  web:
    external: true
